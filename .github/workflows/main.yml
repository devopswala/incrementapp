name: DevSecOps

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: azure
    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: check Kubernetes version
      run: |
        sudo kubectl version

    - name: check docker version
      run: |
        sudo docker version
